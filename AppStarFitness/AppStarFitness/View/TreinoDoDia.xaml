<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppStarFitness.View.TreinoDoDia">
    <ContentPage.Content>
        <StackLayout>
            <Label x:Name="lbl_treino_dia" Text="TREINO DO DIA (Ex: Segunda - COSTAS)" FontSize="20" HorizontalTextAlignment="Center"/>

            <Label Text="Selecione um exercício e clique no botão para mais detalhes" FontAttributes="Italic" FontSize="20" HorizontalTextAlignment="Center"/>

            <ScrollView>
                <ListView x:Name="lst_assoc">
                    <ListView.Header>
                        <Label Grid.Row="0" Grid.Column="0" Text="Exercícios" FontSize="20" HorizontalTextAlignment="Center"/>
                    </ListView.Header>

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <Label Grid.Row="0" Grid.Column="0" Text="{Binding repetitions}" FontSize="20" HorizontalTextAlignment="Center"/>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </ScrollView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>